{
  "name": "claude-notify",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun src/index.ts",
    "build": "bun build --compile --minify --sourcemap src/index.ts --outfile dist/claude-notify"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@clack/core": "1.0.0-alpha.7",
    "@clack/prompts": "1.0.0-alpha.7"
  }
}
